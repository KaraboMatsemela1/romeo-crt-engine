# Phase 6D — Model #1 Closure Exhaustion 001

**Date:** 2026-08-15  
**Tracking:** Issue #102  
**Baseline:** Recovery 007 plus bounded Model #1 passes #89/#91/#94/#96/#98/#100  
**Mode:** evidence synthesis only  
**Issue #16 disposition:** `KEEP_BLOCKED`

## Executive decision

The current first-party corpus is exhausted for **deterministic Model #1 closure**. The completed bounded passes have isolated multiple unresolved fields that cannot be closed by another broad search of the same evidence set.

One remaining item — Episode 1's deictic `stop loss up here` wording — is potentially resolvable by a directly captured technical frame. However, even a successful stop-owner binding would not make `MODEL_1_GEOMETRY` candidate-ready because the qualifying old-extreme selector, deterministic `thick` semantics, and expiry/cancellation remain absent from the current first-party corpus.

Therefore:

```text
MODEL_1_DECISION                    = WAIT_FOR_NEW_MODEL1_EVIDENCE
CURRENT_CORPUS_CLOSURE_LEVERAGE      = EXHAUSTED
INDEPENDENT_CLOSURE_AUDIT            = NOT_READY
MODEL_1_CANDIDATE_READY              = FALSE
NEXT_NEAREST_HELD_PREDICATE           = TRUE_MSS_ALGORITHM
ISSUE_16                              = KEEP_BLOCKED
ISSUE_37                              = MUST_NOT_START
```

No additional broad Model #1 recovery issue should be created unless a new first-party artifact or directly bindable technical frame can answer a specific missing field.

## Sources reconciled

This synthesis reconciles:

- `research/romeo/phase6d/MINIMUM_MISSING_FIELD_CLOSURE_001.md`
- `research/romeo/phase6d/MODEL1_OLD_EXTREME_SELECTOR_RECOVERY_001.md`
- `research/romeo/phase6d/MODEL1_TRIGGER_RETRACE_CONTRACT_001.md`
- `research/romeo/phase6d/MODEL1_THICK_SEMANTICS_001.md`
- `research/romeo/phase6d/MODEL1_INVALIDATION_STOP_OWNER_001.md`
- `research/romeo/phase6d/MODEL1_EXPIRY_CANCELLATION_001.md`
- `research/romeo/phase6d/PREDICATE_LEDGER_V2.json`
- Recovery 007's provenance-bound Model #1 timed-text artifacts.

## Required-field state

`MODEL_1_GEOMETRY` requires:

`EXACT_PREDICATE`, `INFORMATION_AVAILABILITY_TIME`, `DIRECTION_TIMEFRAME_OWNERSHIP`, `CONFIRMATION`, `INVALIDATION`, `EXPIRY`, `DATA_REQUIREMENTS`.

| Required field | Current state | What is directly supported | Remaining debt | Current-corpus action |
|---|---|---|---|---|
| `EXACT_PREDICATE` | `STRONG_PARTIAL` | one-specific-candle object; directional sweep-candle examples; close-trigger relation; directional trigger symmetry | old-extreme selector missing; `thick` causality/measurement unresolved | no broad retry; requires new semantics |
| `INFORMATION_AVAILABILITY_TIME` | `STRONG_PARTIAL` | signal/trigger cannot exist before confirming close is known | exact executable fill policy and lifetime after trigger not source-defined | no closure-unlocking current-corpus action |
| `DIRECTION_TIMEFRAME_OWNERSHIP` | `STRONG_PARTIAL` | monthly→D1, weekly→H4, daily→H1 Model #1 mappings | complete context-direction ownership/scope for an executable candidate | requires additional explicit first-party context |
| `CONFIRMATION` | `DIRECT_SUPPORTED_AT_SIGNAL_LEVEL` | close beyond selected Model #1 candle is directly called the trigger used to enter | full predicate cannot instantiate deterministically because candle/reference qualification remains unresolved | no separate confirmation recovery needed |
| `INVALIDATION` | `PARTIAL / FRAME_BINDABLE_DEBT` | Model #1 worked example directly mentions a stop | exact owner behind `stop loss up here`; setup cancellation distinct from stop | exact chart frame could improve this field only |
| `EXPIRY` | `MISSING` | none in provenance-bound Model #1 ledger | no deterministic cancellation/time boundary | requires genuinely new first-party semantics |
| `DATA_REQUIREMENTS` | `PARTIAL` | timestamped OHLC + timeframe structure are clearly implicated | unknown old-extreme construction and possible `thick` comparison inputs prevent a complete contract | requires semantics first |

## Completed bounded findings

### 1. Qualifying old high / old low

Issue #91 result:

```text
NO_SELECTOR_RULE_FOUND
```

Romeo's first-party `old CRTH` bearish-reaction clarification is real context, but it does not prove that every Model #1 old high is an old CRTH, define `old`, provide a ranking rule, or define the bullish mirror.

**Current-corpus disposition:** exhausted.

### 2. Close-trigger versus retrace

Issue #94 result:

```text
CLOSE_IS_EXECUTION_TRIGGER
MANDATORY_RETRACE_ENTRY = NOT_PROVEN
```

The confirming close is directly source-supported as the semantic trigger. A worked Episode 9 retrade does not make retracement a universal prerequisite.

**Current-corpus disposition:** trigger relation sufficiently clarified; do not re-open broadly.

### 3. `thick` candle semantics

Issue #96 result:

```text
THICK_TERM_USAGE         = DIRECT_REPEATED
THICK_CAUSALITY          = UNRESOLVED
THICK_NUMERIC_DEFINITION = ABSENT
```

The historical `body/full_range >= 0.50` value remains a project-only frozen parameter and receives zero first-party closure credit.

No ATR/body-percentile/range threshold may be invented to fill the gap.

**Current-corpus disposition:** exhausted unless Romeo explicitly defines the adjective.

### 4. Structural stop / invalidation

Issue #98 result:

```text
PARTIAL_DEICTIC_CONTEXT_ONLY
```

Episode 1 directly says `stop loss up here`, but the text does not bind `here` to a named structural object. Episode 9's Turtle-Soup-low stop is directly evidenced only for its worked True-MSS sequence and cannot be transferred into Model #1.

This is the only Model #1 debt with a plausible exact existing-media action:

```text
Capture the technical chart frame corresponding to Episode 1 10:18–10:56
and determine whether the visual unambiguously binds `up here` to a named
Model #1 / swept-extreme object.
```

However, resolving this item alone cannot close Model #1 due the independent semantic absences above.

**Current-corpus disposition:** frame-bindable improvement, but not closure-unlocking. Defer until direct frame capture is available or a wider evidence event justifies the acquisition cost.

### 5. Expiry / cancellation

Issue #100 result:

```text
NO_DETERMINISTIC_MODEL1_EXPIRY_FOUND
```

The ledger has no `EXPIRY` evidence entry for Model #1. The historical v0.1 Candle-3-close rule remains a project formalization only.

**Current-corpus disposition:** exhausted.

## Why another Model #1 broad recovery is not justified

A new broad pass over Episode 1 / Episode 9 captions would repeat already captured material without a defined missing field likely to be answered by that text.

The remaining high-impact debts are not retrieval failures:

- exact old-extreme selector;
- explicit meaning/causality of `thick`;
- deterministic expiry/cancellation;
- full context ownership.

They are **semantic absences in the currently admitted first-party corpus**.

The correct autonomous state is therefore not `search harder`; it is:

```text
WAIT_FOR_NEW_MODEL1_EVIDENCE
```

## Re-entry conditions for Model #1

Re-open targeted Model #1 recovery only if at least one of these occurs:

1. a new first-party Romeo source explicitly defines qualifying old highs/lows for Model #1;
2. Romeo explicitly defines or rejects `thick` as a causal Model #1 condition;
3. a first-party source states Model #1 setup expiry/cancellation;
4. a directly captured chart frame unambiguously binds the Episode 1 stop reference;
5. a new first-party end-to-end Model #1 example provides enough fields simultaneously to materially change the two-engineer test.

Every re-entry issue must target an exact missing field. No generic `research Model #1 more` issue is authorized.

## Two-engineer test

Two independent engineers still cannot produce materially identical Model #1 signals from raw market data because they may legitimately disagree on:

- which old high/low qualifies;
- which candle counts as `thick` or whether that adjective is a hard filter;
- when the setup expires;
- the structural stop/invalidation owner;
- complete context ownership where multiple valid timeframe/context interpretations exist.

Therefore:

```text
TWO_ENGINEER_TEST = FAIL
REQUEST_INDEPENDENT_CLOSURE_AUDIT = NO
```

## Next-nearest predicate

The minimum-closure analysis already ranked:

```text
1. MODEL_1_GEOMETRY
2. TRUE_MSS_ALGORITHM
```

Model #1 is now current-corpus exhausted. The next legitimate research focus is therefore `TRUE_MSS_ALGORITHM`.

Strong direct starting evidence already exists in Recovery 007:

- Episode 9 08:49–09:23: old-low stab then close above `the high that broke the low`, followed by a retracement area;
- Episode 9 15:23–17:01: lower-timeframe worked sequence, 5m SMT context, break relation, retracement area and Turtle-Soup-low stop example.

The highest-leverage True MSS question remains:

> **How does Romeo deterministically construct the swing / `high that broke the low` reference?**

That should be a separate bounded issue and must not substitute a generic ICT/BOS swing algorithm.

## Final disposition

```text
MODEL_1_GEOMETRY                 = STRONG_PARTIAL
MODEL_1_CURRENT_CORPUS           = EXHAUSTED_FOR_CLOSURE
MODEL_1_NEXT_ACTION              = WAIT_FOR_NEW_MODEL1_EVIDENCE
TRUE_MSS_ALGORITHM               = NEXT_RESEARCH_PRIORITY
CLOSED_PREDICATES                = 0
CANDIDATE_READY_ROWS             = 0
ISSUE_16                         = KEEP_BLOCKED
ISSUE_37                         = MUST_NOT_START
```

This report changes research prioritization only. It does not alter historical Phase 6/6B results, strategy implementation, protected validation state, paper infrastructure authorization, or any trading authorization.
