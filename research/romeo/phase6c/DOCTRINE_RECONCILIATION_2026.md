# Phase 6C — 2025 vs 2026 Doctrine Reconciliation Ledger

**Date:** 2026-08-14  
**Baseline doctrine:** `CRT_SECRETS_2025`  
**Research stream:** `CRTOLOGY_2026_RESEARCH`  
**Status:** **ACTIVE — NO NEW ALPHA CANDIDATE SELECTED**

## Purpose

Track first-party 2026 statements against the frozen 2025 doctrine without silently rewriting historical strategy rules.

Each row is classified before any detector activity or P&L is opened.

Allowed classifications:

```text
CLARIFICATION
REFINEMENT
NEW_OPTIONAL_BRANCH
SUPERSEDING_RULE
NON_ALPHA_CONTEXT
UNRESOLVED
```

A source-supported statement is not automatically executable. `Deterministic? = NO` means it cannot enter a strategy order path yet.

## Reconciliation ledger

| 2026 source / clue | 2025 baseline relation | Classification | Deterministic now? | Executable effect |
|---|---|---|---|---|
| CRT and Turtle Soup have not changed; system becomes more refined/robust with more nuances | Core CRT/Turtle Soup already foundational in 2024/2025 corpus | `CLARIFICATION` | YES as versioning principle | Preserve historical core; default new material to refinement unless direct supersession is proven |
| `ROMEO-2026-CRTOLOGY-01` — organised/collective application lens; do not force when absent | 2025 already requires context before lower-timeframe pattern and rejects pattern-first fitting | `REFINEMENT` / `UNRESOLVED` | NO | No new selector until the organised state/predicate is directly defined |
| Episode-1 follow-up — do not fade strong trend without warning signs; follow until it slows | Countertrend/strong-trend regime logic explicitly deferred in Phase 1/2 | `REFINEMENT` / `UNRESOLVED` | NO | Potential future regime/context branch; no metric or warning-sign registry yet |
| Episode-1 follow-up — Tuesday Turtle Soup / Wednesday classic buy-sell / Thu-Fri cap weekly range | Time/context is central in 2025; exact day-of-week state machine was not active in v0.1 | `NEW_OPTIONAL_BRANCH` / `UNRESOLVED` | NO | Preserve as time-context hypothesis; no hard weekday filter authorized |
| `ROMEO-2026-LIVE-02` mall analogy — availability does not mean buy everything | v0.1 architecture already separates candidate availability from qualification/risk authorization | `CLARIFICATION` | YES as principle | Reinforces gated selection; does not define ranking among valid candidates |
| `Every entry is an exit, and every exit is an entry` / `exit-entry theory` | 2025 predeclares entry/target/exit but does not contain this exact structural theory as executable state | `REFINEMENT` / `UNRESOLVED` | NO | No target/entry changes until owning structure and lifecycle are defined |
| `Every quarter is a candle` | CRT fractality exists; broader parent timeframes beyond W1/D1/H4 were deferred | `NEW_OPTIONAL_BRANCH` | NO | Potential quarterly parent route; calendar/execution mapping unresolved |
| Most students should trade Model #1 and OTEs for a long time | Model #1 source-supported; OTE explicitly deferred from v0.1 | `CLARIFICATION` for Model #1; `UNRESOLVED` for OTE integration | PARTIAL | No change to frozen Model #1; OTE remains excluded until deterministic |
| Journey to key level vs reaction from key level | Already explicit in 2025 Episode 5 and Phase 1 taxonomy | `CLARIFICATION` / RESTATEMENT | YES conceptually | Does not reopen v0.1 journey branch; journey execution semantics still separately unresolved |
| Convincing opposite CRT can justify changing bias swiftly | 2025 failure/context work already recognizes direction conflict; broad bias-transition algorithm deferred | `REFINEMENT` / `UNRESOLVED` | NO | Potential dynamic-bias transition branch; `convincing` predicate unresolved |
| `Every quarter is a candle` plus weekly-open emphasis | Existing doctrine treats every candle as a range and emphasizes higher-timeframe context | `NEW_OPTIONAL_BRANCH` / `REFINEMENT` | NO | May motivate multi-scale context research, not current execution |

## Candidate-readiness test

A row may become a Phase-6C candidate delta only if all are true:

```text
DIRECT_FIRST_PARTY_EVIDENCE          = sufficient
EXACT_PREDICATE                      = defined
INFORMATION_AVAILABILITY_TIME        = defined
DIRECTION/TIMEFRAME_OWNERSHIP        = defined
CONFIRMATION/INVALIDATION/EXPIRY     = defined where applicable
POSITIVE_AND_NEGATIVE_FIXTURES       = obtainable without hindsight
DATA_REQUIREMENTS                    = known
NO_OUTCOME_BASED_SELECTION           = true
```

Current result:

```text
candidate_ready_rows = 0
```

## Most promising unresolved branches by evidence relevance

This ordering is based on direct-source relevance and pre-existing research debt, **not** historical profitability or expected signal count.

### A. Dynamic context / bias transition

First-party Romeo material explicitly says a convincing opposite CRT can require changing bias and warns against fading strong trend without signs of slowdown.

Still missing:

- exact `convincing CRT` predicate;
- trend-strength/slowdown predicate;
- timeframe that owns the transition;
- transition expiry;
- interaction with Candle-3 entry state.

Disposition: `RESEARCH PRIORITY — NOT EXECUTABLE`.

### B. Time-context / weekly organisation

Direct first-party 2026 posts provide explicit weekday observations and emphasize weekly planning/open context.

Still missing:

- whether observations are tendencies or mandatory filters;
- exact session boundaries;
- instrument-class scope;
- direction and target semantics;
- relationship to Episode-1 `SS`.

Disposition: `RESEARCH PRIORITY — NOT EXECUTABLE`.

### C. Exit-entry structural theory

First-party source names the concept explicitly, but the causal object is not defined in accessible text.

Disposition: `HIGH INTEREST — SOURCE CAPTURE REQUIRED`.

### D. Quarterly parent/fractality expansion

`Every quarter is a candle` is direct and conceptually consistent with CRT fractality.

Still missing full calendar and execution mapping.

Disposition: `LATER DATA/SPEC BRANCH — NOT EXECUTABLE`.

## Explicit non-candidates

At this stage Phase 6C does **not** select:

- a guessed `SS` implementation;
- a weekday-only strategy;
- an OTE strategy;
- a quarterly CRT strategy;
- a dynamic-bias strategy;
- an exit-entry strategy;
- any lower-threshold version of v0.1/v0.2.

## Current decision

```text
2025 doctrine                    PRESERVED
2026 clues                       VERSIONED AND RECONCILED
new executable delta             NONE YET
new alpha candidate              NOT SELECTED
detector activity                NOT AUTHORIZED
backtest / P&L                   NOT AUTHORIZED
Phase 7                          BLOCKED
```
